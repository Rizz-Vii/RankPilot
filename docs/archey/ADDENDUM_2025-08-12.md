# Addendum – Current Status Alignment (Aug 12, 2025)

This addendum updates production readiness and priorities to reflect the latest repository state.

## Summary of Changes and Status

- Provenance enforcement: universal middleware now applied to dashboards, visualizations, and billing invoice APIs; provenance audit script available.
- Table Data API: Firestore-backed at `dashboardTables/{widgetId}/rows` with deterministic fallback and CSV export; optional `teamId/userId` query scoping.
- Automation scheduling: due-automation scheduled function added; manual `/api/automation/run-due` deprecated (410). Emulator tests pending.
- AI adapter: `functions/src/lib/ai-memory-manager.ts` now supports env-driven provider selection (OpenAI/Gemini/Anthropic) with capability routing and a deterministic mock fallback (guarded by AI_MOCK_FALLBACK and latency budget). Additional dashboards/tests to broaden are planned.
- Finance pages: still using mock metrics; wire to real data source or guard behind feature flag.
- Rate limiting & KPIs: team-aware limiter partial; `/api/health` exposes KPIs and alert thresholds.

## Recommended Next Actions (1–2 sprints)

1. Implement real AI provider adapter (env-driven), preserve mock fallback; add unit tests and observability.
2. Replace finance mocks with real source or gate via env/feature flag; add contract tests.
3. Add emulator tests for scheduled runner; consider task queue for heavy actions.
4. Harden team-scoped rate limiting (token/leaky bucket), expand tests.
5. Remove deprecated stubs/files once references are gone (see `INCOMPLETE_CODE_AUDIT.md`).

## Verification and Health

- `/api/health` includes KPIs and alerts (provenanceCoveragePct, rateLimitRejectionRate, routesP95 map, etc.).
- Table Data API verified to return Firestore rows first, deterministic fallback when empty/unavailable; CSV export consistent with JSON sort.
- Scheduled runner deployed; emulator coverage outstanding.

### Track B Updates

- Any baseline established (see artifacts/lint/any-baseline.json); regression guard script added.
- Visualization & NeuroSEO core typing surfaces introduced.
- Table data JSON/CSV parity test added (organized spec).

### Track C Updates

- Team token bucket headers now included on success responses (flag gated).
- Scheduler emulator backed by new computeNextRun helper; tests stabilized.
- Table data CSV parity spec aligned with contract (format=csv; normalized metric/value/change).
- Brain typings expanded (BrainPlan, ToolInvocation, ValidationResult) reducing explicit any usage.
- TestContext adopted across additional organized specs.
- Any baseline artifact (artifacts/lint/any-baseline.json) to be (re)generated via npm run lint:any-baseline and committed.
