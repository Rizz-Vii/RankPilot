// src/app/(app)/dashboard/page-backup.tsx (deprecated backup page)
// Fully removed legacy implementation. Keeping a minimal stub to avoid build errors.
// If a historical version is ever needed, retrieve it from git history.
"use client";

export default function DashboardBackupPage() {
  return null; // Intentional no-op: legacy dashboard superseded by current implementation.
}

// NOTE: Do not reintroduce legacy code here. This file exists only as a placeholder to
// prevent accidental routing or imports from breaking during refactors.
