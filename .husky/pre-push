#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

# Validate delegation log (non-blocking)
if ! npm run validate:aider-log >/dev/null 2>&1; then
  echo "⚠️  Aider delegation log validation failed (non-blocking). Run npm run validate:aider-log manually." >&2
fi

exit 0
